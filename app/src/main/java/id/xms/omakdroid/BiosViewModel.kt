package id.xms.omakdroid

import androidx.lifecycle.ViewModel

class BiosViewModel : ViewModel() {
    // BiosViewModel is now a simple placeholder
    // All hardware detection is done directly in the Composable
    // No download or extraction logic - that's handled by BootSplashViewModel
}
